# Copy to a local, ignored file before use. Never commit real paths or keys.
policy_model:
  name: qwen3-4b
  source: local_or_openai_compatible
  path: /path/to/Qwen3-4B
  endpoint: http://127.0.0.1:8000/v1
  revision: pin-before-run

teacher_or_evaluator:
  enabled: false
  name: optional-large-model
  path: /path/to/teacher-or-evaluator
  purpose: offline_generation_or_audit_only

embedding_and_reranker:
  embedding_model: all-MiniLM-L6-v2
  reranker_model: cross-encoder/ms-marco-MiniLM-L-6-v2

